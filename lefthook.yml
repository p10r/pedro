pre-commit:
  parallel: true
  commands:
    test:
      run: go test ./...
      glob: "*.go"
    golangci-lint:
      run: golangci-lint run
      glob: "*.go"
    nil-away:
      run: nilaway -include-pkgs="github.com/p10r/pedro" ./...
      glob: "*.go"
