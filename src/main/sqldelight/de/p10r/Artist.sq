CREATE TABLE IF NOT EXISTS stored_artists(
  id INTEGER PRIMARY KEY NOT NULL,
  name TEXT NOT NULL
);

create {
  INSERT INTO stored_artists (name)
  VALUES (?)
  RETURNING id;
}

selectAll:
SELECT *
FROM stored_artists;

findByName:
SELECT *
FROM stored_artists
WHERE lower(name) = lower(?);
