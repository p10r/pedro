CREATE TABLE stored_artists(
  id INTEGER PRIMARY KEY NOT NULL,
  name TEXT NOT NULL
);

create {
  INSERT INTO stored_artists (name)
  VALUES (?);

  SELECT *
  FROM stored_artists
  WHERE id = last_insert_rowid();
}

selectAll:
SELECT *
FROM stored_artists;
